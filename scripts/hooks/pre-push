#!/usr/bin/env bash
# Rechaza push cuando lo intenta un agente (PSP_AGENT=1). Instalar: scripts/install-hooks.sh
if [ "${PSP_AGENT:-0}" = "1" ]; then
  echo "pre-push: los agentes no hacen push. Un humano revisa y fusiona." >&2
  exit 1
fi
